include: package:flutter_lints/flutter.yaml

formatter:
  trailing_commas: preserve

linter:
  rules:
    - prefer_const_constructors

analyzer:
  exclude:
    - build/**
    - android/**
    - ios/**
    - macos/**
