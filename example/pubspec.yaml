name: pausable_stream_iterable_example
description: Flutter demo app for pausable_stream_iterable (Rick & Morty).
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  pausable_stream_iterable:
    path: ../
  dio: ^5.10.0
  json_annotation: ^4.12.0

dev_dependencies:
  flutter_lints: ^6.0.0
  build_runner: ^2.15.0
  json_serializable: ^6.14.0

flutter:
  uses-material-design: true
